default-locale: en-US
engine: jolt

jolt:
  jobs:
    # max-jobs: 2048
    # max-barriers: 8
    # num-threads: 0
    # temp-allocator-size: 20971520
  spaces:
    # max-bodies: 65536
    # max-body-pairs: 65536
    # max-contact-constraints: 16384
    # num-body-mutexes: 0
    # collision-steps: 1
    # integration-sub-steps: 1

physx:
  # num-threads: 0

bullet:
  # ?

delta-time-multiplier: 1.0

engine-timings:
  buffer: 60.0
  buffers-to-display: [ 5.0, 15.0, 60.0 ]

primitive-models:
  box: { material: minecraft:stone }
  sphere: { material: minecraft:stone }
