package io.github.aecsocket.rattle.paper

import io.github.aecsocket.alexandria.paper.AlexandriaCommand

internal class RattleCommand(val rattle: Rattle) : AlexandriaCommand(rattle) {
    init {
    }
}
